import styled from 'styled-components';

export const WrapperHeaderFull = styled.header`
  width: 100%;
  display: block;
  position: relative;
  border-bottom: var(--uxu-border-default);
  background-color: var(--uxu-color-primary-background);
`;
